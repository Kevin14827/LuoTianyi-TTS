# ref/

参考音频目录。**音频文件不进仓库**（见 `.gitignore`）。

推理时需要一段 3–10 秒的干净人声：

- `ref.wav` — 无背景音乐、无明显混响、单人声
- `prompt.txt` — 与 `ref.wav` 逐字对应的文字（用于对齐音色）

`synth.py` 默认读这两个文件名，也可以用 `--ref` / `--prompt` 覆盖。

参考音频质量直接决定输出质量，换一段参考通常比调参数管用。
